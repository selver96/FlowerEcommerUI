import React from 'react';
import './style.css'

const Loader = () => {
    return (
        <section className="loader">
            <span className="loader_left"></span>
            <span className="loader_right"></span>
        </section>
    );
};

export default Loader;